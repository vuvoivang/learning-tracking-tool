export interface Loan {
  id: number;
  name: string;
  product_name: string;
  is_default: true;
  min_amount: number;
  max_amount: number;
  durations: Array<number>;
  duration_unit: string;
  interest_rate_table: string;
  min_age: number;
  max_age: number;
  min_salary: number;
  max_salary: number;
  allowed_location_ids: Array<number>;
  allowed_district_ids: Array<number>;
  allowed_payment_type_ids: Array<number>;
  allowed_job_ids: Array<number>;
  allowed_dmp_location_ids: Array<number>;
  allowed_dmp_age_range_ids: Array<number>;
  form_sources: ['string'];
  allowed_social_insurance_ids: Array<number>;
  allowed_company_tax_codes: ['string'];
  allowed_company_tax_codes_url: string;
  allowed_labor_contract_ids: Array<number>;
  allowed_location_ids_excel_url: string;
  allowed_new_location_ids_excel_url: string;
  allowed_unsecured_loan_ids: Array<number>;
  allowed_income_types: Array<number>;
  product_code: string;
  product_logo: string;
  config_form: string;
  partner_banner: string;
  state: 'ACTIVE';
  created_at: number;
  updated_at: number;
  allowed_audience_ids: Array<number>;
  hotline: string;
  form_image_url: string;
}
